exp_name: "extractor_'@{extractor.name}'-aggregator_'@{aggregator.name}'-ucp_'@{ucp.name}'"
output_dir: ./results/results_audio_EVAL/
save_output: True
merge_option: default
mark: default

min_length_audio_signal: 20

pipeline:
  name: unsupervised_audio_inference_v1

